# Safari Bot Data

Replace these CSV files when Safari Portal exports change:

- `pages_area.csv`: area pages used for RI location tags
- `pages_property.csv`: property pages used to match entered hotel names
- `pages_transfer.csv`: transfer pages used to match From Area, To Area, and Segment

Keep the filenames the same so Safari Bot reloads them automatically.
